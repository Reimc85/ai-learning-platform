{
  "name": "ai-learning-platform",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "engines": {
    "node": "18"
  }
}
